# Availability

Employees may maintain:

- recurring availability rules
- date-specific exceptions

Managers can review and override availability where authorized.

Maximum hours and availability are hard scheduling constraints. General preferences are not treated as hard constraints unless explicitly configured.
