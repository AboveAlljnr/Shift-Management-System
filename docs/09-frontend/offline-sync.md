# Frontend Offline Sync

IndexedDB stores pending attendance events.

The client must display:

- pending
- syncing
- synced
- failed/requires review

The UI must never imply an attendance action was server-confirmed until synchronization succeeds.
